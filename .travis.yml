language: python
dist: xenial

python:
  - '3.6'
  - '3.7'

env:
  global:
    - PIPENV_IGNORE_VIRTUALENVS=1

install:
  - 'pip install pipenv'
  - 'pipenv install --dev'

before_script:
  - yarn danger ci

script:
  # - 'pipenv run black --check life tests'
  - 'pipenv run pytest'